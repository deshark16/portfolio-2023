export type ContentList = {
  id1: number,
  id2: number,
  children1: JSX.Element,
  children2: JSX.Element,
  gap?: boolean,
  textLeft?: boolean,
  textRight?: boolean,
  mediaLeft?: boolean,
  mediaRight?: boolean,
}